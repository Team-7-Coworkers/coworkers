import { Canvas, Meta, Story } from '@storybook/blocks';
import Image from 'next/image';

import { SolidDefault as ButtonBasic } from './Button.stories';
import { Basic as CircleGraphBasic } from './CircleGraph.stories';
import { Default as CalendarBasic } from './CustomCalendar.stories';
import { Default as DropdownBasic } from './Dropdown.stories';
import { Circle as ImageUploadBasic } from './ImageUpload.stories';
import { Default as InputBasic } from './Input.stories';
import { Basic as LoadingBasic } from './Loading.stories';
import { Box as TextFieldBasic } from './TextField.stories';

<Meta title="Coworkers - 공통 UI 컴포넌트" />

<div className="sb-container">
  <div className="sb-section-title">
    # Coworkers - 공통 UI 컴포넌트

    Coworkers 서비스를 팀 프로젝트로 진행하면서 공통으로 사용하는 UI 컴포넌트를 모아놓은 라이브러리입니다. Storybook을 사용하여 공통 UI 컴포넌트를 확인하고 사용하는 방법에 대해 설명하려 합니다.

  </div>

  <div className="sb-section">
    <div className='flex-1'>
      <div className="sb-section-item">
        ## Button
        <Canvas of={ButtonBasic} />
        <p className="sb-section-item-paragraph">버튼 컴포넌트 입니다. `styleType`, `state`, `size` 등의 속성을 지원합니다. [자세히 보기](?path=/docs/components-button--docs)</p>
      </div>

      <div className="sb-section-item">
        ## CustomCalendar
        <Canvas of={CalendarBasic} />
        <p className="sb-section-item-paragraph">달력 컴포넌트 입니다. `onDateSelect`, `selectedDate` 속성을 지원합니다. [자세히 보기](?path=/docs/components-customcalendar--docs)</p>
      </div>

      <div className="sb-section-item">
        ## ImageUpload
        <Canvas of={ImageUploadBasic} />
        <p className="sb-section-item-paragraph">이미지 업로드 컴포넌트 입니다. `onUploadSuccess`, `onUploadError`, `url` 등의 속성을 지원합니다. [자세히 보기](?path=/docs/components-imageupload--docs)</p>
      </div>

      <div className="sb-section-item">
        ## Loading
        <Canvas of={LoadingBasic} />
        <p className="sb-section-item-paragraph">로딩 컴포넌트 입니다. `text`, `classname`, `iconClassname` 등의 속성을 지원합니다. [자세히 보기](?path=/docs/components-loading--docs)</p>
      </div>
    </div>

    <div className='flex-1'>
      <div className="sb-section-item">
        ## CircleGraph
        <Canvas of={CircleGraphBasic} />
        <p className="sb-section-item-paragraph">도넛 그래프 컴포넌트 입니다. `rate`, `classnames` 속성을 지원합니다. [자세히 보기](?path=/docs/components-circlegraph--docs)</p>
      </div>

      <div className="sb-section-item">
        ## Dropdown
        <Canvas of={DropdownBasic} />
        <p className="sb-section-item-paragraph">드롭다운 컴포넌트 입니다. `children`, `className` 속성을 지원합니다. [자세히 보기](?path=/docs/components-dropdown--docs)</p>
      </div>

      <div className="sb-section-item">
        ## Input
        <Canvas of={InputBasic} />
        <p className="sb-section-item-paragraph">인풋 컴포넌트 입니다. `type`, `placeholder`, `value` 등의 속성을 지원합니다. [자세히 보기](?path=/docs/components-input--docs)</p>
      </div>

      <div className="sb-section-item">
        ## TextField
        <Canvas of={TextFieldBasic} />
        <p className="sb-section-item-paragraph">텍스트필드 컴포넌트 입니다. `type`, `placeholder`, `value` 속성을 지원합니다. [자세히 보기](?path=/docs/components-textfield--docs)</p>
      </div>
    </div>

  </div>
</div>

<style>
  {`
  .sb-container {
    margin-bottom: 48px;
  }

  .sb-section {
    width: 100%;
    display: flex;
    flex-direction: row;
    gap: 20px;
  }
  .sb-section a {
    display: inline-block;
  }
  .sb-section a:hover {
    text-decoration: underline;
  }

  .sb-section-title {
    margin-bottom: 32px;
  }

  .sb-section a:not(h1 a, h2 a, h3 a) {
    font-size: 14px;
  }

  .sb-section-item, .sb-grid-item {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .sb-section-item-paragraph {
    margin: 0;
    margin-bottom: 20px;
    padding-bottom: 10px;
  }

  @media screen and (max-width: 600px) {
    .sb-section {
      flex-direction: column;
    }
  }
  `}
</style>
